# 洛天依展示站（Cloudflare Workers）静态资源放置说明

本项目会把 `Dancer/public` 作为静态资源目录上传到 Cloudflare Workers，并由 Worker 做静态资源转发与单页回退。

## 目录结构

```
Dancer/
  public/
    index.html
    styles.css
    app.js
    assets/
      images/
        hero.jpg
        portrait.jpg
        gallery/
          01.jpg
          02.jpg
          ...
          12.jpg
      audio/
        bgm.mp3
```

## 资源命名规则（按默认代码）

- 头图背景：`Dancer/public/assets/images/hero.jpg`
  - 用于整站背景模糊大图，建议横图，1920×1080 或更大
- 侧边人像：`Dancer/public/assets/images/portrait.jpg`
  - 用于首页右侧卡片，建议竖图，4:5 比例（如 1200×1500）
- 画廊图片：`Dancer/public/assets/images/gallery/01.jpg` ~ `12.jpg`
  - 缺失的图片会自动隐藏，不需要凑齐 12 张也能正常显示
  - 想放更多张：你可以照着 `index.html` 里的按钮块继续复制新增（`13.jpg`、`14.jpg`…）
  - 尺寸建议：优先用横图（3:2 / 16:9），例如 1600×1067、1920×1080；主体尽量放中间
  - 原因：画廊卡片固定高度并用裁切铺满（cover），横图在桌面端裁切最少；竖图会被裁得更明显
- 背景音乐：`Dancer/public/assets/audio/bgm.mp3`
  - 页面里有 audio 控件；右上角“播放/暂停”按钮会控制同一个音频

## 注意事项

- 自动播放限制：多数浏览器会阻止自动播放音频，所以需要用户点击“播放”后才会开始。
- 路径写法：页面里使用相对路径（例如 `assets/...`），可以兼容 Worker 挂载在子路径（route 不是根路径）的情况。
- 访问路径：现在支持把站点挂在任意子路径下（例如访问 `/tianyi/` 或 `/anything/`），静态资源也能正常命中。
- 文件体积：图片建议压缩（webp/jpg），音频建议控制体积（例如 128~192kbps mp3），避免首次加载过慢。
- 文件名大小写：Cloudflare 上大小写敏感，建议严格按上面的文件名与扩展名放置。

## 本地预览与部署（Workers Paid）

在 `Dancer/` 目录下执行：

```bash
npm i
npm run dev
```

部署：

```bash
npm run deploy
```

如果你没有登录过 Cloudflare：

```bash
npx wrangler login
```
